package testNG_MKT;

import org.testng.annotations.Test;

public class TwoTest2 
{
  @Test(invocationCount=10)
  public void login()
  {
	  
  }
 
}
// we can run the same test case in multiple times we can use the parameter in @Test is invocationCount.